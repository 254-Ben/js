let data = {
  name: "John",
  location: "Berlin",
  profesion: "Designer"
};

// john is a Designer and lives in Berlin
console.log(
  `${data.name} is a ${data.profession} and lives in ${data.location}`
);
